type ContactMessage @model @auth(rules: [{allow: public, provider: apiKey, operations: [create]},
  {allow: private, operations: [read, update, delete]}])
{
  name: String!
  email: String!
  subject: String!
  message: String!
  status: String @default(value: "NEW")
  userAgent: String
  ipAddress: String
}